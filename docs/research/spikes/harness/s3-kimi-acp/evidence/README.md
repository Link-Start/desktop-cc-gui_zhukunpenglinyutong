# Evidence policy

Raw ACP transcripts and replay payloads are intentionally not committed because
they can contain session identifiers, tool input/output, and host details. Re-run
the probes in `../probes/` to generate local evidence.

The repository retains only the reviewed conclusions and the hardened harness.
