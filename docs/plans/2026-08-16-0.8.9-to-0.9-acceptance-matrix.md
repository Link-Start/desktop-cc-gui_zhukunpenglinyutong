---
type: acceptance-matrix
created: 2026-08-16
source_branch: bump-version-0.8.9
target_branch: cxn-version-0.9
source_tip: 71b42b1b7
target_tip: ba8fd6415
working_tree: dirty-uncommitted
unique_commits: 62
---

# 0.8.9 → 0.9 验收对照表

采样：`bump-version-0.8.9=71b42b1b7`，`cxn-version-0.9` tip=`ba8fd6415`，**工作区有未提交收口**（以 WT 为准，不是 tip）。  
**核对方式**：打开 0.9 现码路径，看用户能不能走完。**没有手测过的一律不标「可验收通过」。文件存在 ≠ 已接线。**

## 标记

| 标记 | 含义 |
|---|---|
| **已接线** | 0.9 现码对上了 0.8.9 这条的行为合同。我没替你点过 UI。 |
| **部分** | 有入口或缺一段闭环，验收必挂。 |
| **未搬** | 0.9 没有对应能力。 |
| **不搬** | 文档/revert，或 0.8.9 自己后来推翻（如 rail）。 |
| **仅文档** | 只改 openspec / preview / 研究笔记。 |

验收请按 **用户能做什么** 测，不要按 commit 号点。

---

## A. 62 条提交逐条

| # | commit | 0.8.9 改了什么 | 0.9 校准 | 标记 |
|---|---|---|---|---|
| 1 | `e2c2aa97e` | 过期 AskUserQuestion 不当成 workspace 断连 | `expired_claude_ask_request_id` + FE classifier 在 | 已接线 |
| 2 | `5eda694d5` | 闸门结算前 drain live-text 尾 | `settleThreadWaitingForUserChoice` 调 `drainLiveAssistantTextTail` | 已接线 |
| 3 | `4c36e2ad8` | CLI MCP 超时 +30s | `ASK_USER_QUESTION_CLI_TIMEOUT_MARGIN_SECS=30` | 已接线 |
| 4 | `8f9bd1a0d` | 闸门前先 flush 再 drain | `flushPendingRealtimeEventsRef` 在 drain 前 | 已接线 |
| 5 | `9c738dd47` | 选项卡主题化勾选 | `UserInputOptionCheck` + CSS | 已接线 |
| 6 | `160a69f33` `d5a591941` `43ddbaed7` `94246ea46` | 上述 Ask 的单测 | rust 过期路由 + 无 hint 过期 vitest + 闸门 flush→item-delta drain→text drain 顺序测 + wall-clock 提交 + 选项卡 ✓/`aria-pressed` 已补 | 已接线 |
| 7 | `897b62b0b` | 从本 change 拿掉 broadcast 扩容 | 无独立合同 | 不搬 |
| 8 | `7b4fbe131` `1b7b14681` `557a38c0f` `ddfc41d05` `6a260f530` `9beed6cfe` `949cdf85e` `acbf4242b` | OpenSpec 登记/验收记录 | 0.9 未归档这些 change | 仅文档 |
| 9 | `e4eb86f1e` | Windows 打开方式 `std_command` | powershell / where 已改 | 已接线 |
| 10 | `71b42b1b7` | `Box::pin` + Windows 8MB 栈 | 五处 pin 在；`build.rs` 8MB 0.9 原本就有 | 已接线 |
| 11 | `b1f8ba7dc` | tool output 字节预算 + Codex 垃圾目录忽略 | `boundToolOutput` 已进 live assembler + reducer；`temp/tmp/.tmp` 已进 junk。托管 `.codexignore` 仍未搬 | 部分 |
| 12 | `c63942451` | 托管 codexignore 合并防越界 | 0.9 已无 `merge_managed_codexignore` 路径 | 不搬 |
| 13 | `7c35be932` | run-status 延迟样式 | ComposerToggle `loadSubagentStyles`；run-status 自己拉 `todo-list.css` + `status-panel.plan-list.css` | 已接线 |
| 14 | `a88c77a5b` | Diff 弹窗滚动 + 拆 compare CSS | `git-history-diff-modal-viewer` `min(72vh)` + `loadDiffStyles` 多两个 css | 已接线 |
| 15 | `16ddbc907` | 批量终端完成后自动折 | `BashToolGroupBlock` `wasProcessingRef` | 已接线 |
| 16 | `0493aa387` | live 尾部过程窗口折 | `TRAILING_PROCESS_*` 在 viewModel | 已接线 |
| 17 | `94245ce99` | 打开历史先画最新 + 芯片加载更早 | 内存 tail-first（不再自动灌满）+ `pendingOlderHistory` 芯片 + Claude 磁盘窗 80 | 已接线 |
| 18 | `b76a8fe8b` | 三层 history IO 预算（含磁盘窗） | Claude 磁盘尾窗已接；消息搜索已按 0.8.9 限「已打开 + 当前会话、上限 12」。侧栏 list 已补回 64KB/40 行早停、不扫 subagent。其它引擎 `load_*_session` 仍全量 load（**0.8.9 同样无磁盘窗**） | 部分 |
| 19 | `2a0ec7d43` | Intent Canvas 时间分组首页 | `IntentCanvasHome` + `eraGrouping` 已挂 | 已接线 |
| 20 | `cd9869976` `7cff96bde` `98c222708` | Canvas/Diff/Pi 预览图 | 不进运行时 | 仅文档 |
| 21 | `6bab0b60e` | 幕布 HTML 链开内置浏览器 | 地球按钮 + `requestBrowserDockOpenUrl` | 已接线 |
| 22 | `8101c0944` | Browser Dock 嵌中心分屏 | 顶栏开关 + `displayMode=embedded` + 分屏层 | 已接线 |
| 23 | `72ff654c7` | 网页摘录折叠 + 指哪定位 | Fold + locate `copySafeText` + identity upsert + `has-browser-excerpt` 已接线。documentPosition / 邻居 / cssPath / 重复点选去重合同已补回。**未手测真 webview** | 部分 |
| 24 | `d5573183f` | 标签右键批量关闭 | 前端菜单 + en/zh/其它 locale + vitest 英文键已接。**未手测真 webview** | 部分 |
| 25 | `edd55bec6` | 内嵌标签渲染关联 + 右键 | 前端 + Rust overlay + tab-context 测试已绿。**未手测真 webview** | 部分 |
| 26 | `ddec3fd57` | 标签菜单隔离与授权 | 随 0.8.9 `browser_agent/mod.rs` 进了 0.9。**未手测真 webview** | 部分 |
| 27 | `7cb9de77e` `53ac07962` | Dock OpenSpec / 原型 | | 仅文档 |
| 28 | `0889e65c8` | PI 一等引擎 | 探测/发送/picker/history/安装器/`piBin`/校验页 doctor 已接线；capability 矩阵补回 PI `--thinking` + `@file`；`ensureModels` 含 `pi`；首页 create 走 Atomic 并落到 `pi-pending-*`。顶栏/侧栏/`onThreadStarted` 不再把 PI 认成 Codex。**未手测真 PI CLI** | 部分 |
| 29 | `605c8133f` | Native PI 续聊 + 历史进侧栏 | `listPiSessions` + first-paint Index 无 PI 时回退刷盘 + timeout debug。**未手测重启侧栏** | 部分 |
| 30 | `653db9eee` | 重启后 Index 投影 Native PI | writer + FE `pi:` prefix + cwd-dir fingerprint 已接。**未手测 8s fresh 窗口外新 jsonl** | 部分 |
| 31 | `3d5f851c5` | Shared 第六个 CLI = PI | FE 白名单 + v2 四臂 + catalog `__local_pi__` + create 不拉 vendor 列表 + picker/`resolveDefaultCreation` 把 `__local_pi__` 当 disk。旧 `shared_sessions.rs` send 仍拒 PI（**0.8.9 同样拒**） | 部分 |
| 32 | `e4c46a3c2` | 协作白名单含 PI | ComposerToggle / StageTargetPicker / `validate_agent_target` 含 Pi；FE + Rust 白名单单测已补回。**未手测协作发 PI** | 部分 |
| 33 | `d7980cd88` | PI 供应商认证 + OAuth 终端 | 认证区块 + `pi_auth_*` + 终端事件 + 校验页 doctor tab 已接。两段式 PTY（先 `pi` 再 `/login`）workspace-flows 合同已补回。**未手测 OAuth / 安装器全量** | 部分 |
| 34 | `a371b75da` | PI 图片 `@file` | `pi.rs` 有 `@file` argv；capability `image.input=supported` 已回矩阵 | 已接线 |
| 35 | `ac9e77e54` | prompt 内 `@path` 抽到 argv | `extract_at_file_references` 在 `pi.rs` | 已接线 |
| 36 | `297ae1659` `ea3ab76a7` `00712d4c9` `354ea2ddf` | PI OpenSpec 归档 | 0.9 未同步主 spec | 仅文档 |
| 37 | `d82b82f81` | 侧栏 Index 唯一快路径 + CLI 分轨 | 0.8.9 **后来自己拆掉 rail**（见 39）。first-paint 暖读 Index（不强制 writer sync）+ Gemini/Grok/Kimi/PI 磁盘 list 默认关。Gemini/Grok/PI writer 已改回并行。full-catalog 仍扫 Claude/catalog（与 0.8.9 相同） | 部分 |
| 38 | `ea409ec00` `db15fa4cd` | Index 后台导入 + 合并刷新 | importer + `session-index-imported` → `mergeExistingThreads` 已接。full-catalog 仍走 Claude/catalog | 部分 |
| 39 | `3a8e0de4c` | **还原扁平列表** + Index 历史回填 | 0.9 本就扁平。importer 尾部已挂 Claude/Codex/Kimi + Gemini/Grok/PI 游标回填。**未手测全历史收敛** | 部分 |
| 40 | `54e4a32b2` | keyset 真分页一页 20 | first-paint 可落下 `session-index::` cursor；load-older 只走 Index keyset（limit+1 探页）。默认可见 root 仍是 5（与 0.8.9 相同）；catalog 回落只在 cursor 本身是 catalog | 部分 |
| 41 | `21e808938` | Shared 侧栏 Index 可见性 | list IPC 同带 `visibility`；未 verified 不画普通 native；shared 行 first-paint 保留 | 已接线 |
| 42 | `43da5a85d` `b6d791a25` | engine-rail 缩进 / 父链类型 | rail 已被 39 推翻；0.9 无 rail | 不搬 |
| 43 | `416bbae5e` | 类型收窄 + 归档会话降级 | 0.9 `mergeNativeCliSessionSummaries` 已含 `"pi"`；归档加载失败走 `null`（`applySessionArchiveState` 区分失败 / 无记录）。rail 类型修补随 rail 推翻不搬 | 已接线 |
| 44 | `9a4e8fb67` | revert 无关 rustfmt | | 不搬 |

---

## B. 按用户验收清单（请按这个测）

下面才是你「挨个找麻烦」该用的表。标记是我对代码路径的判断。

| ID | 你怎么测 | 我标 | 已知会挂 / 要注意 |
|---|---|---|---|
| A1 | Claude 问你题，选一项，倒计时别跳 | 已接线 | 未手测 |
| A2 | 超过 30 分钟再答，应是过期，不是「未连接」 | 已接线 | 未手测 30min |
| A3 | 闸门弹出时，上面那段字还在 | 已接线 | 热路径，请重点看 |
| A4 | 选项选中有 ✓ | 已接线 | |
| A5 | Windows：打开方式探测不闪黑窗 | 已接线 | 我这边没 Windows |
| A6 | 协作 pill 点开有样式不是裸字 | 已接线 | |
| A7 | 长 Git Diff 弹窗能滚到底 | 已接线 | |
| A8 | 批量 bash 跑完自动折 | 已接线 | |
| A9 | 打开超长会话，先看到最新，不是从第一条刷 | 已接线 | 内存只画尾窗；点芯片再 prepend。Claude 磁盘 80；其它引擎仍全量读盘 |
| A10 | 设管理搜索会话，别卡死全盘扫 | 已接线 | 必须显式 exhaustive |
| A11 | 尾部一长串工具卡，超 5 张只留 3 张+芯片 | 已接线 | 未手测直播 |
| A12 | Intent Canvas 列表按本周/更早分组 | 已接线 | 未手测打开/删 |
| A13 | 幕布点 `.html`，**中间分屏 Dock** 打开该页 | 已接线 | `openHtmlInBrowser` 现锁 `open-dock`→`open-url` + pending URL；AppShell persist `layout.browserDockOpen`。**未手测真 webview** |
| A14 | 顶栏地球打开中心 Dock，能输网址 | 已接线 | 请确认设置里没关掉该控件 |
| A15 | Dock 标签右键关其它页 | **部分** | 菜单 i18n + `tab-context-action` 单测已绿。**没跑过真 webview** |
| A16 | Dock 点选网页元素进对话 | **部分** | `selectElement` i18n + chrome toggle 单测已绿。**没跑过真 webview** |
| A17 | 供应商里有 **PI**，能填 API key、能装 CLI | 已接线 | 没在本机装过 npm 包 |
| A18 | picker 选 PI，发一句能回 | 已接线 | 新建 `pi-pending-*`（`continueSession:false`）+ `__local_pi__` 当 disk 不是托管 + `ensureModels("pi")` 拉 catalog + 本机 `pi --print` 回 `pong`。**未点 GUI picker** |
| A19 | 重启后侧栏还有 PI 会话，能续 | 已接线 | first-paint 暖读 Index；PI send 后 invalidate 会让下次 list 补扫；resume `pi:` 走 `loadPiSession`。请重启验证 |
| A20 | Shared 创建可选 PI | 已接线 | create 不拉 vendor 列表，落到 `__local_pi__` + disk。**未手测 Shared 新建** |
| A21 | PI 认证里点「登录」弹出终端跑 `pi /login` | 已接线 | 请实点 |
| A22 | 侧栏 Index 一次 20 条、加载更早继续翻 | 部分 | first-paint 按引擎配额取页，Claude 不再饿死 PI；keyset 翻页不触发 writer。默认可见 root 仍是 5（与 0.8.9 相同） |
| A23 | 打开超长 Claude 会话只读尾窗 | 已接线 | 窗 80 条；非 Claude `load_*_session` 0.8.9 也无 limit |

---

## C. 收口状态（代码路径，2026-08-16 工作区）

**用户路径已接线、单测绿、未手测 UI：**

- PI：picker / 发送 / 续聊 / Index 投影 / Shared 第六 CLI / 协作白名单 / 认证页 / `@file` `@path` / `__local_pi__` dispatch receipt 归一成 `local`
- Dock：中心分屏 CSS 岛/恢复条、Composer FIFO 队列、摘录 locate + identity upsert、NoteCard Globe、`layout.browserDockOpen` 持久化、幕布 HTML 走 dock；旧蓝卡 CSS 已清；A15/A16 菜单与点选 i18n + 单测已绿
- Wave 1：Ask 倒计时不冻、闸门 flush/drain、Windows `std_command`、`boundToolOutput` 进 live 热路径、bash 自动折、Diff 滚动 + `navigationEpoch`、run-status 自己拉 plan/todo CSS
- 历史：tail-first 不再自动灌满 + 芯片 prepend；Claude resume `limit: 80`
- Index 导入后 `session-index-imported` merge 刷新（不替换 0.9 catalog 语义）
- Shared 首屏 hide-set：list IPC `visibility` + 未 verified 不画普通 native
- Index 历史回填：importer 尾部 Claude mtime offset / Codex 日期分区 / Kimi 行 offset / Gemini·Grok·PI 分页递增
- Index load-older：first-paint 可落 `session-index::` keyset；cursor 是 session-index 时只走 SQLite，不再默默扫 catalog
- first-paint Index：`syncIfNeeded=false` + writer 占窗时 800ms 暖读重试；Gemini/Grok/Kimi/PI 磁盘 `list_*` 默认 opt-in 关闭
- Wave 1 测试合同：闸门 flush → item-delta drain → text drain；过期 Ask 无 hint 也走 stale tombstone
- junk 目录：`temp/tmp/.tmp` 已进 `is_special_build_artifact_dir_name`（托管 `.codexignore` 仍未搬）
- 归档降级：加载失败 `null`，不把「读失败」当成「无归档」
- 本轮对照 0.8.9 热路径：PI send/resume/Index/Dock A15–A16 剩余 diff 是 0.9 live-channel / AppShell 改写，不是漏接线。`EngineManager::list_pi_sessions` 0.8.9 也无调用方
- 本机 `pi 0.84.1 --print --mode json --no-session --no-tools` 已回 `pong`；`parse_pi_stream_line` 锁了 session / thinking_delta / text_delta / turn_end
- HTML/`requestBrowserDockOpenUrl` 派发 `open-dock` 后，AppShell 必须 `setBrowserDockOpen(true)` 并切 `chat`（源码合同已锁）
- picker 新建 PI：`startThreadForWorkspace({ engine: "pi" })` 落 `pi-pending-*`，不走 daemon `startThread`
- resume PI：`pi:` 走 `loadPiSession`，不进 Codex `resumeThread`；history loader 去掉 `pi:` 前缀再读盘
- `__local_pi__` 不得当托管渠道：首页 create / Shared create / ModelSelect / session lifecycle 一律归一成 disk / 无 managed binding
- pending 首发：`engine:"pi" continueSession:false sessionId:null`，回 native id 后再 invalidate Index
- 协作白名单：`isMultiAgentTargetSupported("pi")` + Rust `validate_agent_target` 接受 Pi、拒 Gemini
- HTML 打开：`openHtmlInBrowser` 走真实 `open-dock`→`open-url` 事件链并写 pending URL，不再只 mock 函数
- Dock 持久化：AppShell `layout.browserDockOpen` 读写源码合同已锁
- PI capability：fixture/脚本补回第六引擎；`EngineFeatures::pi().reasoning_effort=true`（`--thinking`）；`image.input` + `reasoning.effort` 生成物与单测已绿
- PI 模型表：`parse_pi_models_output` 补回 thinking/vision 描述、首条 default、空表 fallback `auto`；单测已绿
- Wave 1 Ask：补回 wall-clock 过期提交（不 `advanceTimers`）+ 选项卡 ✓/`aria-pressed` 合同
- 删会话：`createDeleteThreadForWorkspaceAction` 先 `tombstoneSessionIndexRows`；`pi:` 走 `delete_pi_session`，不再误删 Codex。批量 `deleteWorkspaceSessions` 也先 tombstone
- PI thinking live：`emitReasoning*` 的 `engineHint` 重新接受 `"pi"`，与 `inferGeminiReasoningHintFromThreadId` 对齐
- Index list fallback 漏了 `tombstoned_at IS NULL`：删掉的 PI/其它会话会从路径归一化回退窗复活。已按 0.8.9 补回；`pi:` 墓碑单测已绿
- PI writer 合同：`rows_from_pi_summaries` 标题/engine、cwd 子目录 jsonl 改 fingerprint、fresh/mismatch/invalidate 单测已补回
- first-paint Index 漏了 `workspace_index_sources_invalidated`：有 Claude 行时 PI send 后的 invalidate 不会触发补扫。已按 0.8.9 接回
- first-page 全局 `LIMIT` 会让最近 Claude 行饿死 PI。已按 0.8.9 改成每引擎配额；`pi-old` 不被 8 条 Claude 挤掉
- keyset「更多」不再顺带跑 writer；首页 `hasMore` 用 `count > 本页行数`
- Wave 1 长会话：迟到 Shared projection 会冲掉 `pendingOlderHistory`，芯片消失。已按 0.8.9 用缓存全量 ⊕ projection + `replacePendingOlderHistoryItems`
- Claude 磁盘窗：`load_claude_session` 的 `hasMore/nextCursor` 已写进 snapshot meta，并在无内存窗时落到 `setThreadHistoryWindow`。芯片点磁盘 cursor 仍不读盘（**0.8.9 同样只写 window，没有 `before` 二次 load**）
- 首页 create picker 并进 Shared 子集后，`handleCreationTargetChange` 会静默丢掉非 Shared 引擎。已按 0.8.9 拆回 `isAtomicExecutionTarget`；PI 本地 disk 目标 Atomic+Resolved 单测已绿
- `ensureModels` 漏了 `pi`：首页选 PI 会空 catalog、seed 失败。已按 0.8.9 补回；create-session 拉 `__local_pi__` 模型单测已绿
- 首页发 PI：Composer 产出 disk `createSessionTarget` + AppShell `startThreadForWorkspace({ engine:"pi", providerProfileId:null, providerProfile:null })` 合同已锁
- Dock A16 locate：0.9 测掉了 documentPosition / 邻居 / cssPath / 重复点选去重。实现还在，合同已按 0.8.9 补回
- 幕布 HTML：`useFileLinkOpener` 相对/绝对路径进内置浏览器 + 无 workspace toast 已补回
- 设置页「全部禁用」漏了 `pi`：会把 PI 留在已启用组。测试已按 0.8.9 补 `pi`，启用列表顺序含 PI CLI
- PI OAuth 两段式终端：设置页点「登录」发 `mossx:terminal-command-request`（先 `pi`，1.5s 后再 `/login <provider>`）。实现还在，0.9 测掉了 workspace-flows 合同，已按 0.8.9 补回
- Wave 1 Ask 选项卡：`UserInputQuestionCard` 仍写 `aria-pressed`；对话框单测被 0.9 删了，已补回。取消/关闭现在多带 `skippedQuestionIds`（0.9 改写，不是漏接线）
- Wave 1 搜索 IO：0.9 删了 `messageSearchScope`，统一搜索会线性扫全部已加载会话。已按 0.8.9 补回「只扫已打开 + 当前会话、上限 12」，并接到雷达热路径
- Index 软同步：0.9 把 Gemini/Grok/PI writer 改成串行。Gemini 3s 超时会把 PI 挤出 first-paint。已按 0.8.9 改回 `tokio::join!`
- Wave 1 Claude 列表 IO：0.9 Preview 只有 40 行、没有 64KB 字节预算，侧栏还扫 subagent jsonl。稀疏大文件会把 8MiB 尾巴读完。已按 0.8.9 补回早停 / 64KB / 不扫 subagent，预算单测 4/4 绿
- PI 引擎识别：顶栏 `resolveEngineType`、`onThreadStarted` 归属、侧栏标题、native 合并 fallback 把 PI 丢掉，会显示成 Codex。已按 0.8.9 补回 `pi` / `pi:` 推断；顶栏 PI 图标/标签单测已绿。`sidebarSnapshot` 0.8.9 同样不写 `pi`，不发明
- PI 可用引擎合同：实现已含 PI（`isEngineExecutionEnabled` + registry），但 0.9 测把 `ENABLED_ENGINE_TYPES` / `availableEngines` / `image.input` 漏掉。已按 0.8.9 补回；42/42 绿
- PI 显示名：`EngineType::display_name()` 和 daemon 桥写成 `"PI"`，Shared 标签会短一截。已按 0.8.9 改回 `"PI CLI"`；`engine_type_display_names` 已锁。退出路径两边都不调 `shutdown_pi_sessions`，不发明
- Shared 校验 / 合同：canonical validator 把 `pi` 当未知引擎拒掉；Shared pending/established/local-target 和 runtime settle 测循环漏了 PI。已按 0.8.9 补回；validator 2/2、Shared 4/4 绿。rewind 两边都不含 PI，不发明
- Dock A16 文案：中文定位被 0.9 改成「文档/上文/下文/祖先」，摘录折叠键在 zh-TW/es/fr/hi/ja/ko/pt-BR/ru 被删。已按 0.8.9 改回「文档坐标/上一条/下一条/所在分组」，并补回各语言 `browserContextExcerpt*`
- 设置页会话管理：引擎过滤丢掉 Kimi/Grok/PI，用户筛不到 PI 会话。已按 0.8.9 补回选项和各语言 `projectSessionEngineKimi/Grok/Pi`
- 多语言 PI 文案：zh-TW/es/fr/hi/ja/ko/pt-BR/ru 缺 `enginePi`、providers `pi` 标签、`piDescription` / `piCliLifecycleHint`。已按 0.8.9 补回
- 设置页安装器 doctor：`onInstallerDoctorResult` 漏了 `engine === "pi"`，PI 安装结果会写进 Claude doctor。已按 0.8.9 补回
- VendorSettings PI tab 合同：0.9 删了「不是即将开放」单测。已按 0.8.9 补回；VendorSettings 55 + SettingsView 27 = 82/82 绿
- 校验页 PI tab：Gemini 隐藏测没锁 PI CLI。已补「PI CLI tab + Run PI Doctor」
- 费用汇总：`costAggregate` 漏了 `pi`，PI 用量不会进引擎聚合。已按 0.8.9 补回
- Composer `engineToProvider`：选 PI 会默认成 Claude，输入框 provider 错绑。已按 0.8.9 补回 `case "pi"`；adapter 62/62 绿
- 搜索/通知点 PI 会话：`isEngineType` 不认 `pi`，`setActiveEngine` 不切回去。已按 0.8.9 补回 workspace-flows + 额度目标；31/31 绿
- PI 会话当 Codex：`toConversationEngine` / 思考去重 / composer effort 规范化漏了 PI。已按 0.8.9 补回；`inferConversationEngineFromThreadId` 也认 `pi:` / `pi-pending-`
- 会话别名：`isFinalizedNativeThreadId` 漏了 `pi:`，正式 PI 会话会被当成 pending 再写 alias。已按 0.8.9 补回
- Catalog 投影：`normalizeCatalogEngine` 漏了 `pi`，PI 行会塌成 Codex。已按 0.8.9 补回；helpers 49/49 绿
- last-good 连续性：`THREAD_ENGINE_SOURCES` 漏了 `pi`，重启/超时回退会丢掉 PI 行。已按 0.8.9 补回；flatten 2/2 绿
- 创建 PI 会话加载文案：`useCreateSessionLoading` 漏了 `engine === "pi"`，会显示成 Claude Code。0.8.9 已有 `workspace.enginePi`，按一等引擎合同补回；create-session 4/4 绿
- 手动恢复：`inferManualRecoveryEngine` 漏了 `pi:` / `pi-pending-`，无 catalog metadata 时会当 Codex 新建。已补回；无 metadata 的 `pi:` 恢复合同已锁。recovery 25/25 绿

**仍未闭环（不能标完成）：**

- 用户路径未手测：GUI picker 发 PI / 续聊 / 重启侧栏；Dock 真 webview 中间分屏 / A15 / A16；Windows A5
- 正在跑的 `/Applications/ccgui.app` 是 8 月 14 安装包，**不是这份工作区**；点现网 GUI 验不到本轮改动
- 0.8.9 终态也不是「侧栏只读 SQLite」。full-catalog 仍扫 Claude/catalog；默认可见 root 仍是 5
- 非 Claude `load_*_session` 0.8.9 也无磁盘窗；不能发明 API
- Daemon PI sync 仍裸 `get_or_create_pi_session`（**与 0.8.9 相同**）
- `onCloseBrowserDock` 进 layout bag 后无消费端（**0.8.9 同样死 prop**）
- 托管 `.codexignore` upsert 未搬（0.9 无该宿主）

`cargo check --lib` 过；`--tests` 仍挂既有 OpenSpec 示例 JSON，与本次无关。  
我没替你点 UI。测的时候直接说 **B 表 ID + 现象**。
